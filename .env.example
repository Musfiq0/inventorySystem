# Environment variables for development and production deployment

# Development MySQL Configuration (XAMPP)
DefaultConnection=Server=127.0.0.1;Database=InventoryManagement;User=root;Password=;Port=3306;AllowUserVariables=true;Convert Zero Datetime=True;

# Production Environment (for Render deployment)
# DATABASE_URL=mysql://username:password@host:port/database
# ASPNETCORE_ENVIRONMENT=Production

# Note: In production on Render, DATABASE_URL is automatically provided by the platform
# and ASPNETCORE_ENVIRONMENT is set via render.yaml configuration